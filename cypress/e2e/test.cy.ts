//describe('Test initial start.', () => {
// it('Visits the app root url and navigates through onboarding', () => {
// cy.visit('/');

// We're now on the onboarding page, add a school
//cy.get('input[placeholder="Name der Schule eingeben"]', {
//  timeout: 10000,
// }).type('Berufsschule');
// cy.contains('ion-button', 'Hinzufügen').should('be.visible').click();

// FIXME
// // Wait for the school to be added and displayed in the list
// cy.contains('ion-item', 'Berufsschule', { timeout: 5000 }).should('exist');
//
// // Continue to the home page
// cy.contains('ion-button', 'Fortfahren').click();
//
// // Now we should be on the home page
// cy.get('ion-content', { timeout: 10000 }).should(
//   'contain.text',
//   'Notenübersicht',
// );
// cy.contains('ion-item', 'Berufsschule').should('exist');
// });
//});
